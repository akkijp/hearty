<%= render 'header' %>

<% url = edit_users_auth_password_reset_url(@account.reset_password_token) %>
<p>パスワードリセットの依頼を受け付けました。</p>

<p>こちらからパスワードの再設定を行ってください。
<br><%= link_to url, url %></p>

<%= render 'footer' %>
